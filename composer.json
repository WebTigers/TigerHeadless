{
    "name": "webtigers/tiger-headless",
    "description": "Headless (non-interactive) installer for Tiger — one spec in, one JSON result out. The install authority behind the web installer, the WHM plugin and Softaculous.",
    "type": "library",
    "license": "BSD-3-Clause",
    "homepage": "https://github.com/WebTigers/TigerHeadless",
    "require": {
        "php": ">=8.1",
        "ext-json": "*",
        "ext-pdo": "*",
        "ext-zip": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-0": { "Tiger_Headless_": "src/" }
    },
    "bin": ["bin/tiger-headless"],
    "config": { "sort-packages": true }
}
